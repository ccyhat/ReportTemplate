<Window x:Class="ReportTemplate.MainWindow.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReportTemplate.MainWindow.Views"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:vm="clr-namespace:ReportTemplate.MainWindow.ViewModels"
        xmlns:cm="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="ReportTemplate" 
        Height="450" 
        Width="800"
        WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 区域 1: 顶部工具栏 -->
        <local:RibbonControlView Grid.Row="0" 
                                 cal:View.Model="{Binding RibbonControlViewModel}"/>
        
        <!-- 下方主显示区：分为左右两部分 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <!-- 左侧区域宽度 -->
                <ColumnDefinition Width="5"/>
                <!-- 分割条宽度 -->
                <ColumnDefinition Width="*"/>
                <!-- 右侧内容区域 -->
            </Grid.ColumnDefinitions>
            
            <!-- 区域 2: 左侧 XML 树形结构 -->
            <Border Grid.Column="0" 
                    BorderBrush="#FFCCCCCC" 
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Background="White">
                <ContentControl cal:View.Model="{Binding XMLTreeViewModel}" />
            </Border>

            <!-- 可拖动的分割条 -->
            <GridSplitter Grid.Column="1" 
                          Width="5" 
                          Background="LightGray" 
                          HorizontalAlignment="Stretch" 
                          ShowsPreview="True"/>

            <!-- 区域 3: 右侧编辑区域 -->
            <Border Grid.Column="2"
                    BorderBrush="#FFCCCCCC"
                    BorderThickness="1"
                    CornerRadius="4"
                    Background="White">
                <ContentControl cal:View.Model="{Binding EditControlViewModel}" />
            </Border>
        </Grid>
    </Grid>
</Window>
